import $ from 'jquery';
import React from 'react';

import Shared from '../common/Shared.js';

import './setting.css';

export default React.createClass({
    render: function () {
        return (
            <h1>Settings</h1>
        );
    }
});
