var $ = require('jquery');
var React = require('react');

var Shared = require('../common/Shared.js');

require('./setting.css');

module.exports = React.createClass({
    render: function () {
        return (
            <h1>Settings</h1>
        );
    }
});
