import React from 'react';

import Shared from '../common/shared.js';

import './setting.css';

export default React.createClass({
    render() {
        return (
            <h1>Settings</h1>
        );
    }
});
