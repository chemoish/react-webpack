var $ = require('jquery');
var React = require('react');

var Shared = require('../common/Shared.js');

require('./home.css');

module.exports = React.createClass({
    render: function () {
        return (
            <h1>Home</h1>
        );
    }
});
